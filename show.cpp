#include "func.h"
using namespace std;

/*Mostra na linha de comando os emparelhamentos obtidos e a quantidade
de professores que puderam ser alocados de forma estavel*/
void imprimir_resultado (){

list<Escola>::iterator it;
it = (list<escola>.begin())

while (iterator list<escola> != list<escola>.end()) {
    cout << iterator->pareamento.front() << ' ';
    If(itv->pareamento.back() !=itv->pareamento.front()){
        Cout<<iterator->pareamento.back()}
    iterator list<escola>++;
}
}

// for(list<professor>::iterator it = professor.preferencias.begin(); it != professor.preferencias.end(); it++)
// for(list<escola>::iterator it = escola.pareamento.begin(); it != escola.pareamento.end(); it++)
